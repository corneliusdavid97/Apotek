{% extends 'master' %}

{% block content %}

	<div class="col-md-12 col-md-offset-0">

    		{{ form.open({route: 'admin.create'}) }}
    			{{ form.button('Create','', { class: 'btn btn-success'}) }}
    		{{ form.close() }}

		<table class="table table-striped">
			<thead>
				<tr>
					<th>id</th>
					<th>Create Date</th>
					<th>Update Date</th>
					<th>Name</th>
					<th>userId</th>
					<th>NameL</th>
					<th>Telephone Number</th>
				</tr>
			</thead>
				<tbody>
				{% for admin in admin%}
					<tr>
						<td>{{ admin.id }}</td>
						<td>{{ admin.created_at }}</td>
						<td>{{ admin.updated_at }}</td>
						<td>{{ admin.name }}</td>
						<td>{{ admin.userId }}</td>
						<td>{{ admin.TelpNo }}</td>
						<th><a href="admin/{{admin.id}}">View</a></th>
					</tr>
				{% endfor %}
			</tbody>
		</table>
	</div>

{% endblock %}
